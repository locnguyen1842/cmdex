---
phase: 1
slug: layout-overhaul
status: draft
shadcn_initialized: true
preset: new-york / neutral / css-variables / lucide
created: 2026-04-08
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for Phase 1: Layout Overhaul.
> Generated by gsd-ui-researcher. Covers UIUX-01, UIUX-02, UIUX-04.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (components.json confirmed) |
| Preset | new-york, baseColor: neutral, cssVariables: true |
| Component library | Radix UI (via shadcn) |
| Icon library | lucide-react |
| Font | Inter (sans), JetBrains Mono (mono) — loaded via Google Fonts in style.css |

Source: `frontend/components.json`, `frontend/src/style.css` lines 0–84

---

## Spacing Scale

Declared values (multiples of 4 only). This phase uses the Tailwind 4px base scale. Per D-08: use consistent rhythm, not maximum whitespace.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline badge padding, tag chip gaps |
| sm | 8px | Sidebar content padding, footer padding, section item vertical padding |
| md | 16px | Default element spacing, sidebar header horizontal padding |
| lg | 20px | Main body horizontal padding (existing .main-body: 20px 24px) |
| xl | 24px | Main body horizontal padding outer edge, section breaks |
| 2xl | 32px | Not used in Phase 1 layout areas |
| 3xl | 48px | Not used in Phase 1 layout areas |

**Phase-specific spacing rules (from codebase audit):**

| Area | Property | Value | Source |
|------|----------|-------|--------|
| Sidebar header | padding | 16px 16px 8px | style.css `.sidebar-header` — normalized from 14px 14px 10px |
| Sidebar content | padding | 8px 8px | style.css `.sidebar-content` — normalized from 8px 6px |
| Section header row | padding | 4px 8px | style.css `.sidebar-section-header` — tightened from 5px 8px |
| Command item | padding | 4px 8px | style.css `.command-item` — normalized from 5px 10px |
| Panel separator | style | 1px `var(--border)` | D-09: border not gap/shadow |
| Main body | padding | 20px 24px | style.css `.main-body` — preserve |

Exceptions:
- Sidebar auto-collapse icon rail: 44px minimum touch/click width for the collapsed trigger area (accessibility — icon-only clickable region)
- Tab bar height: 35px fixed (existing `--tab-bar-height`) — do not change
- Header height: 35px fixed (existing `--header-height`) — do not change

---

## Typography

All sizes in px. Base font-size declared in style.css `body`: 13px.

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 13px | 400 (regular) | 1.5 | Sidebar command titles, detail descriptions, output lines |
| Label | 11px | 600 (semibold) | 1.2 | Category section headers (uppercase, letter-spacing 0.6px) |
| UI small | 10px | 400 (regular) | 1.2 | Tag chips, count badges, keyboard shortcut keys |
| Heading | 13px | 600 (semibold) | 1.3 | Command detail title, panel headers |

Font families:
- UI text: `var(--font-sans)` → `'Inter', -apple-system, BlinkMacSystemFont, sans-serif`
- Code/script: `var(--font-mono)` → `'JetBrains Mono', 'Fira Code', monospace`

Source: `frontend/src/style.css` lines 82–84, 296–303, 504–508

---

## Color

Default theme is VS Code Dark+ (`:root` in style.css). All values reference CSS variables so they remain theme-agnostic.

| Role | CSS Variable | Default Hex | Usage |
|------|-------------|-------------|-------|
| Dominant (60%) | `var(--background)` | #1e1e1e | Main content area, active tab background |
| Secondary (30%) | `var(--card)` / `var(--secondary)` | #252526 / #2d2d2d | Sidebar background, tab bar, popover, inactive tabs |
| Accent (10%) | `var(--primary)` | #007acc | Reserved — see list below |
| Destructive | `var(--destructive)` | #f44747 | Delete confirmation button, destructive menu items only |
| Muted | `var(--muted-foreground)` | #858585 | Secondary labels, inactive tab text, icon color |
| Border | `var(--border)` | rgba(255,255,255,0.1) | All panel separators (per D-09) |

**Accent (`var(--primary)`) reserved exclusively for:**
1. Active tab bottom indicator line
2. Active command item left border in sidebar
3. Focus ring on interactive inputs (`var(--ring)`)
4. Status bar background
5. Delete confirmation popover: NOT accent — use `var(--destructive)` for the [Delete] button

**Accent never applied to:** hover backgrounds, general borders, panel headers, inactive states.

Source: `frontend/src/style.css` lines 39–85 (`:root` theme block)

---

## Component Inventory

Components used or modified in this phase. No new shadcn components need to be added.

| Component | Location | Phase 1 Change |
|-----------|----------|----------------|
| `ResizablePanel` | `frontend/src/components/ResizablePanel.tsx` | Add `window.resize` listener; auto-collapse at ≤600px threshold (D-01, D-03) |
| `Popover` | `frontend/src/components/ui/popover.tsx` | Use existing install — inline delete confirmation (D-04, D-06) |
| `AlertDialog` | `frontend/src/components/ui/alert-dialog.tsx` | Retain for non-delete modals (discard unsaved, clear history, CategoryEditor) |
| `Sidebar` | `frontend/src/components/Sidebar.tsx` | Move delete confirmation from App.tsx modal to local Popover state |
| `App.tsx` | `frontend/src/App.tsx` | Remove `setModal()` path for `confirmDelete` — replace with prop/event to Sidebar |

Sidebar collapsed render: show existing `<svg>` logo icon in `collapsedIcon` prop (from Specifics section of CONTEXT.md). No new icon needed.

---

## Interaction Contracts

### Sidebar Responsive Collapse (UIUX-01)

| Trigger | Behavior |
|---------|----------|
| Window width ≤ 600px | ResizablePanel auto-collapses to icon rail |
| Window width > 600px after auto-collapse | Does NOT auto-expand — user must expand manually |
| User clicks logo icon in collapsed rail | Panel expands to stored width |
| User manually collapses | Stores collapsed state in localStorage (existing behavior) |
| Auto-collapse | Also updates localStorage so refreshed state is consistent |

Collapsed rail minimum width: 44px (icon click target).
Resize listener: debounced at 100ms to avoid layout thrash.

### Inline Delete Confirmation (UIUX-02)

| Trigger | Behavior |
|---------|----------|
| User selects "Delete" from ContextMenu on command or category | Popover opens anchored to trigger element |
| Popover open | Shows label "Delete?" + red [Delete] button + [Cancel] button |
| User clicks [Delete] | Calls `onDeleteCommand` / `onDeleteCategory` from App.tsx; Popover closes |
| User clicks [Cancel] or presses Escape | Popover closes; no action |
| User clicks outside Popover | Popover closes; no action |

Popover positioning: Radix auto-positioning (`side="top"` preferred, falls back automatically). No custom positioning.
[Delete] button: `variant="destructive"` matching existing `AlertDialogAction variant="destructive"` visual.
[Cancel] button: `variant="ghost"` or `variant="outline"`.

State ownership: Popover open/close state lives in Sidebar component (local state), NOT in App.tsx ModalState. The `confirmDelete` ModalState case in App.tsx is removed after migration.

### Spacing Pass (UIUX-04)

Spacing pass targets four areas per D-07:

| Area | Change |
|------|--------|
| Sidebar density | Section header row: tighten vertical padding from 5px to 4px. Command item gap: keep 8px. Logo margin-bottom: 8px (from 10px). |
| CommandDetail controls | Header padding: audit and apply consistent 20px top/sides matching `.main-body`. Control row gaps: 8px between icon buttons. |
| OutputPane / HistoryPane panel headers | Header padding: 8px 12px. Item row vertical padding: 8px 12px. Consistent across both panes. |
| Panel gaps (sidebar/content/output) | All panel separators: 1px `var(--border)` solid. No box-shadow. No visible gap. Matches D-09. |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (Phase 1 has no new CTAs) | Not applicable — Phase 1 is layout-only |
| Sidebar empty state (no commands) | "No commands yet" / "Add a command to get started" |
| Sidebar empty category | "No commands in this group" |
| Delete confirmation popover label | "Delete?" |
| Delete command confirmation button | "Delete" |
| Delete category confirmation button | "Delete" |
| Cancel delete | "Cancel" |
| Collapsed sidebar tooltip (logo icon hover) | "Expand sidebar" |

**Destructive action confirmation copy (D-04):**
- Delete command: Popover label "Delete?" + button "Delete" (no further description — command name visible from context)
- Delete category: Popover label "Delete?" + button "Delete" (category name visible from trigger context)
- No secondary warning text inside the popover — consistent with VS Code compact confirmation patterns

Error states in Phase 1 (layout only, no new async operations):
- No new error states introduced. Existing error patterns (toast from sonner) remain unchanged.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | popover (existing install), alert-dialog (existing install) | not required — already installed |
| Third-party | none | not applicable |

Source: `frontend/components.json` — `"registries": {}` (empty). No third-party registries declared.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

*Phase: 01-layout-overhaul*
*UI-SPEC created: 2026-04-08*
*UI-SPEC revised: 2026-04-08 — fixed typography weights (4→2) and normalized spacing to 4px multiples*
*Sources: 01-CONTEXT.md (7 decisions), REQUIREMENTS.md (UIUX-01, UIUX-02, UIUX-04), style.css tokens, components.json*
